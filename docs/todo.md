# todo - exp-mbt-chasm-kmp

Current state: **Milestones 1 and 2 are both complete.** `increment(41) = 42` runs end-to-end on both an iOS simulator (`iosApp/`, Kotlin/Native via `Shared.framework`) and an Android emulator (`androidApp/`, ART via a KMP `androidTarget`) — the project's core question (one Chasm/KMP host for iOS *and* Android) now has real evidence on both platforms, not just iOS plus a desktop-JVM proxy for Android. Call overhead is measured on `jvm`, `iosSimulatorArm64`, and Android across both a scalar call (`increment`) and a heap-boxed-tuple return (`move_point`); every number clears the same "modest 2D game" headroom bar `exp-mbt-wasmkit-ios` set.

---

## Background

`exp-mbt-wasmkit-ios/docs/todo.md` surveyed [`CharlieTap/chasm`](https://github.com/CharlieTap/chasm) — a Wasm interpreter written in Kotlin Multiplatform, documented as legal on both iOS and Android — as an alternative to the "Swift+WasmKit on iOS, Java/Kotlin+Chicory on Android" per-platform split that project and [`Nanaloveyuki/wasee-moon`](https://github.com/Nanaloveyuki/wasee-moon) each independently reached. It declined to pursue Chasm there, since doing so would mean the iOS host app itself is written against Kotlin Multiplatform instead of native Swift — a materially different application architecture than that project's Swift/WasmKit scope.

This project is that evaluation, carved out separately so the decision doesn't have to be made inside `exp-mbt-wasmkit-ios`.

## Decisions made before any code existed

- **Name**: `exp-mbt-chasm-kmp`, not `mbt-chasm-cmp`. The goal is validating Chasm's Kotlin/Native and Kotlin/JVM call behavior, not Compose Multiplatform UI sharing; naming it `-cmp` would have silently expanded scope to include UI-layer feasibility, which is not the question being asked.
- **`guest/` is a self-contained MoonBit module** (its own `moon.mod`), not part of a repository-root `moon.mod` the way `exp-mbt-wasmkit-ios` is structured. That project's whole repository *is* the MoonBit module, with `ios/` as the secondary consumer; this repository's primary build system is Gradle/KMP, so the root should not also claim to be a MoonBit module root. `guest/` plays the same wasm-guest role either way — only the location of `moon.mod` differs, reflecting which side is primary in each project.
- **Milestone 1 scope is (B): actual iOS integration, not just a Kotlin/Native binary.** Considered scoping Milestone 1 to (A) — confirm Chasm's generated bindings work on a Kotlin/Native target at all, with no Xcode packaging — before touching iOS app integration, mirroring how `exp-mbt-wasmkit-ios` separated "is this technically feasible" (Milestone 1) from "does it integrate into an actual iOS app" (Milestone 2). That separation was warranted there because iOS-target code generation itself was in doubt (native/llvm backend's triple support, `libmoonbitrun.o` being macOS-only). Kotlin/Native → iOS has no equivalent doubt; it's an established, mainstream KMP use case. The only open question is Chasm-specific (does its interpreter/binding generation work correctly once embedded in an iOS app bundle), so testing it only via a bare Kotlin/Native binary wouldn't actually de-risk what's in doubt. Decided to keep (B) as the milestone goal, but preserve the failure-isolation benefit of (A) by ordering the checklist within Milestone 1:
  1. Generate Chasm bindings for a MoonBit `wasm` export and call them from an `iosSimulatorArm64` Kotlin/Native test, no Xcode packaging.
  2. Only after that passes, package `shared` into an `iosApp/` Xcode project and confirm the same call on a simulator.
- **No `publish.yml` (mooncakes) for now.** `exp-mbt-wasmkit-ios` carries one despite its own `SECURITY.md` describing itself as "not a published library" — a leftover from porting `starlark-mbt`/`urllib-mbt` boilerplate without re-evaluating fit for an experimental repo. This project is even more clearly a validation spike, so the inconsistency wasn't carried forward. Can be added later if `guest/` ever becomes a reusable package worth publishing.

## Milestone 0: Project scaffold

- [x] Ported `AGENTS.md`/`CLAUDE.md`, `.markdownlint.json`, `.pre-commit-config.yaml`, `apm.yml`, `.github/` (CI, Copilot setup, issue/PR templates), `justfile`, `LICENSE`, `CODE_OF_CONDUCT.md`, `CONTRIBUTING.md`, `SECURITY.md`, `README.md` from `exp-mbt-wasmkit-ios`, adapting each for this project's inverted structure (Gradle/KMP-primary, `moon.mod` inside `guest/` rather than at the repository root) and narrower scope (no `publish.yml`; see decisions above)
- [x] `git init` and initial commit, then the rest committed in concern-based commits (AGENTS/CLAUDE, tooling config, CI, GitHub templates, community health files, decision log) — done by the user and this agent together
- [x] Created the GitHub remote (`connect0459/exp-mbt-chasm-kmp`, private) and pushed — done by the user; CI initially failed twice on the fresh remote (a missing `guest.wasm` resources directory on a clean checkout, and `dorny/paths-filter` needing `fetch-depth: 0` on a private repo), both fixed
- [x] Ran `apm install` to populate `apm_modules/`/`.claude/skills` from `apm.yml`; `apm.lock.yaml` committed (both `apm_modules/` and `.claude/` stay gitignored, per this file's own `.gitignore`)

## Milestone 1: Minimal Chasm round trip (MoonBit `wasm` → Chasm → Kotlin/Native call, then iOS simulator)

Goal: verify whether Chasm can load a MoonBit-compiled `wasm` module, generate Kotlin bindings for it at build time, and call it correctly — first from a bare Kotlin/Native test, then packaged into an iOS simulator app — before deciding whether this route is viable as `exp-mbt-wasmkit-ios`'s Milestone 2 replacement for Android support.

- [x] Scaffolded `guest/` as its own MoonBit module via `moon new` (probed its default output first in a scratch directory to confirm current field names, e.g. `preferred_target = "wasm"` is already the generated default), then stripped what didn't fit a single-purpose guest module: no `cmd/main` executable package, no nested `AGENTS.md`/`LICENSE`/`README.mbt.md` (the repository root already carries these; `exp-mbt-wasmkit-ios/guest` itself has no per-package README either, consistent with "not a general-purpose library")
- [x] TDD: wrote `guest_test.mbt` first (Red — confirmed `moon test --target wasm` fails with "The value identifier increment is unbound"), then implemented `increment(value : Int) -> Int` with `#export_name("increment")` in `guest.mbt` (Green) — mirrors `exp-mbt-wasmkit-ios/guest`'s first export exactly, so any difference in Chasm's behavior later is attributable to the host, not the guest function
- [x] `moon info`, `moon fmt`, `moon check --deny-warn --target all`, `moon test --target all` all pass (js / wasm / wasm-gc / native)
- [x] `moon build --target wasm --release` then `wasm2wat` confirms the same trivial shape `exp-mbt-wasmkit-ios` found for `increment`: `(func (param i32) (result i32) local.get 0 i32.const 1 i32.add)`, no MoonBit GC/runtime overhead
- [x] `pre-commit run --all-files` passes with `guest/` staged (the `moon-fmt`/`moon-check`/`moon-test` local hooks correctly scope to `guest/` via their `cd guest &&` entries and `^guest/.*` file filter)
- [x] Scaffolded the KMP Gradle project: `settings.gradle.kts` + root `build.gradle.kts` (group `dev.connect0459.mbtchasmkmp`) + `gradle/libs.versions.toml` (Kotlin 2.4.20, matching the locally installed compiler), `shared/` module targeting `jvm()` and `iosSimulatorArm64()`. Generated the Gradle wrapper via `gradle wrapper --gradle-version 9.7.1` rather than hand-writing it
  - TDD: wrote `PlatformTest.kt` calling `platformName()` first (Red — `./gradlew :shared:jvmTest` fails with "Unresolved reference 'platformName'"), then added the classic KMP `expect fun platformName(): String` (commonMain) with `actual` implementations in `jvmMain`/`iosSimulatorArm64Main` (Green) — this is a scaffold-health probe, not the Chasm binding surface itself; deliberately not named `increment` so it isn't confused with the guest export once Chasm is wired in
  - `./gradlew :shared:jvmTest` and `./gradlew :shared:iosSimulatorArm64Test` both pass. The first `iosSimulatorArm64Test` run downloaded the Kotlin/Native compiler + LLVM/sysroot bundle (one-time, ~1m13s); subsequent runs will be faster
  - Local note (not committed): this sandbox's plain `java`/`./gradlew` invocation couldn't locate a JRE via the usual mechanisms (`/usr/libexec/java_home` also failed) even though a JDK was installed via Homebrew and the standalone `gradle` CLI found it fine; had to export `JAVA_HOME` explicitly per command. Likely a sandbox-shell-only quirk (no shell profile sourced), not a project configuration problem — no fix applied to the repo itself
- [x] Added a Kotlin linter/formatter to `shared/build.gradle.kts`, a local pre-commit hook, and a `shared-lint` CI job — see the "Kotlin linter" entry below for why this took two attempts
- [x] Added `shared-test` (`:shared:jvmTest :shared:iosSimulatorArm64Test`) job to `ci.yml`, gated on a new `shared` path filter, running on `macos-latest` (an `iosSimulatorArm64` build needs Xcode, unavailable on `ubuntu-latest`) — resolves the placeholder comment left there when `guest/` CI was first written
  - Caught and fixed a mistake before committing: the first draft pinned `actions/setup-java` to a fabricated commit SHA (looked plausible, wasn't real). Verified the actual tag SHA via `git ls-remote --tags` before using it — a reminder to verify any hash-pinned action rather than pattern-matching one
- [x] **Added Chasm's Gradle plugin** (`io.github.charlietap.chasm.gradle` `2.2.0-1.7.0`, verified against the Gradle Plugin Portal's metadata rather than trusting the README) to `shared/build.gradle.kts`, pointed at `src/commonMain/resources/guest.wasm` — resolves the first open question below. Codegen (`codegenModuleCommonMainGuestService`) produces `GuestService` (interface: `fun increment(p0: Int): Int`) and `internal class GuestServiceImpl(binary: ByteArray, ...)`; no suspend factory since `generateSuspendingFactories` wasn't set
  - `guest.wasm` build/copy automated via `just build-guest-wasm` (mirrors `exp-mbt-wasmkit-ios`'s identically-named recipe) and gitignored as a build artifact (`shared/src/commonMain/resources/*.wasm`), same treatment as that project's `ios/Resources/*.wasm`
  - The raw wasm bytes still have to be supplied by the caller at runtime (`GuestServiceImpl`'s `binary` parameter) — Chasm's own multiplatform example only demonstrates this for jvm/js/wasmJs (no iOS target in that example at all), via a hand-rolled `expect`/`actual` `BinaryLoader` reading JVM classpath resources, which doesn't translate to Kotlin/Native. Instead of building a per-platform resource loader, added a Gradle task (`generateGuestWasmBytes`) that embeds `guest.wasm` as a `ByteArray` literal in a generated `commonMain` source file — works identically on every target with no resource/bundle lookup, and this doesn't change once `iosApp/` exists either
  - TDD: wrote `GuestServiceTest.kt` calling `GuestServiceImpl(GUEST_WASM_BYTES).increment(41)` first (Red — `Unresolved reference 'GUEST_WASM_BYTES'`, since the embedding task didn't exist yet), then added `generateGuestWasmBytes` (Green)
  - **Verified on both targets**: `./gradlew :shared:jvmTest` and `./gradlew :shared:iosSimulatorArm64Test` both pass `incrementReturnsItsArgumentPlusOne` — this is the milestone's core deliverable
- [x] **Kotlin linter, take two**: the original choice (`org.jlleitschuh.gradle.ktlint` 12.1.2) could not be made to skip Chasm's generated sources — tried the extension-level `filter { exclude { ... } } }` DSL and a task-level `PatternFilterable.exclude` directly on `BaseKtLintCheckTask`, both silently no-op. Turned out to be a known, still-open upstream bug (JLLeitschuh/ktlint-gradle#746, #751) affecting generated Kotlin Multiplatform sources specifically. Found that Chasm's own build hits the identical problem and works around it by using a different linter, `kotlinter` (`org.jmailen.kotlinter`), with the same task-level exclude technique (`tasks.withType<ConfigurableKtLintTask>().configureEach { exclude {...} }`) — switched to match, and it worked immediately. Renamed accordingly: pre-commit hook `ktlint-format` → `kotlin-format` (`./gradlew formatKotlin`), CI step `ktlint check` → `Kotlin lint (kotlinter)` (`./gradlew lintKotlin`)
- [x] **Investigated how Chasm handles a heap-boxed/tuple return**, mirroring `exp-mbt-wasmkit-ios`'s `move_point` spike exactly
  - Added `move_point(x, y, dx, dy) -> (Int, Int)` to `guest/` (TDD: Red — `move_point is unbound` — then Green), and added `options(link: { "wasm": { "export-memory-name": "memory" } })` to `guest/moon.pkg` (same option `exp-mbt-wasmkit-ios` needed for WasmKit, needed again here for Chasm)
  - `wasm2wat` confirms the identical shape `exp-mbt-wasmkit-ios` found: MoonBit heap-allocates an 8-byte boxed tuple and returns a pointer (`i32`), `x'` at offset 0 and `y'` at offset 4 — this is a MoonBit codegen characteristic, independent of which wasm host reads it
  - Regenerated Chasm's codegen and inspected the output directly: `GuestService.movePoint(p0: Int, p1: Int, p2: Int, p3: Int): Int` — a plain `Int` return. Chasm's codegen has function-level config for string returns (`stringReturnType()`, several encoding strategies) but nothing analogous for a raw struct/tuple pointer; it only ever sees a wasm signature `(i32,i32,i32,i32) -> i32` and has no MoonBit-specific (or general boxed-value) awareness
  - **The generated `GuestServiceImpl` can't be used to decode the pointer**: it encapsulates its `store`/`instance` privately, and a memory pointer is only meaningful within the exact store/instance that produced it. Decoding requires bypassing the generated wrapper entirely and using Chasm's public low-level embedding API directly: `module(bytes)` → `store()` → `instance(store, module, imports)` → `invoke(store, instance, "move_point", listOf(NumberValue.I32(...), ...))` → take the returned `Int` as a pointer → `exports(instance).first { it.name == "memory" }.value as Memory` → `readInt(store, memory, pointer)` / `readInt(store, memory, pointer + 4)`
  - This needs `io.github.charlietap.chasm:chasm` (the runtime library) as an explicit dependency (originally added to `commonTest`; moved to `commonMain` once `GuestBenchmark`'s `move_point` measurement needed it too, since it isn't test-only anymore) — the Gradle plugin only pulls in what its own generated code needs, not this lower-level API
  - **Verified**: `GuestMemoryTest.movePointReturnsAPointerToAHeapAllocatedTupleInLinearMemory` passes on both `jvm` and `iosSimulatorArm64`, decoding `move_point(10, 20, 3, -5)` back to `(13, 15)`
  - **Conclusion**: Chasm is no better or worse than WasmKit here — both require the same manual pointer/linear-memory decoding for MoonBit's boxed-tuple returns, because this is a MoonBit-side characteristic (not something either host's codegen abstracts away). Chasm's codegen is a convenience layer for scalar-in/scalar-out (and string) exports; anything else still needs its low-level API, same as `exp-mbt-wasmkit-ios`'s hand-written `GuestBridge.swift` needed WasmKit's low-level memory access
- [x] **Scaffolded `iosApp/`**, a Tuist-managed Xcode project, using Kotlin's officially documented "direct integration" method (no CocoaPods) rather than guessing at Xcode build settings
  - The first thing fetched for this — a generic WebFetch of the docs URL — came back empty and the model filled in a plausible-looking but unverified answer (invented `-PXCODE_CONFIGURATION=...` flags, manual Framework Search Paths). Didn't trust it: found kotlinlang.org's multiplatform docs are sourced from a separate repo, `JetBrains/kotlin-multiplatform-dev-docs` (not `JetBrains/kotlin-web-site`, which no longer carries them), and read `topics/development/multiplatform-direct-integration.md` directly at that repo's `master` ref — the invented flags don't appear in the real doc at all
  - Added `binaries.framework { baseName = "Shared" }` to `shared`'s `iosSimulatorArm64` target — required for the `embedAndSignAppleFrameworkForXcode` Gradle task to register at all
  - `GuestServiceImpl` is `internal` (Chasm's codegen convention, matching the pattern its own `ConsumerProvider.kt` example uses); added a small public `createGuestService(): GuestService` factory in `commonMain` so Swift has something to call. TDD'd (Red: unresolved `createGuestService`, Green: the one-line factory)
  - `iosApp/Project.swift` (Tuist): a `.pre` Run Script phase running the doc's exact script (including the `OVERRIDE_KOTLIN_BUILD_IDE_SUPPORTED` guard for IDE-driven builds), `basedOnDependencyAnalysis: false`, and `ENABLE_USER_SCRIPT_SANDBOXING: "NO"` (the doc's required Build Setting — the task fails under Xcode's script sandboxing otherwise). No `Tuist/Package.swift` needed this time — unlike `exp-mbt-wasmkit-ios`'s WasmKit dependency, nothing here comes through SPM
  - `iosApp/Sources/ContentView.swift` calls `GuestServiceFactoryKt.createGuestService().increment(p0: 41)` directly — no Swift-side bridge file needed (unlike `exp-mbt-wasmkit-ios`'s `GuestBridge.swift`), since Kotlin/Native's Swift export already gives a typed, idiomatic call site
  - **Verified end-to-end**: `tuist generate`, `xcodebuild ... build` (succeeded on the first attempt — the doc's steps were sufficient as written), then `simctl install`/`launch`/`io screenshot` on `iPhone 17` (iOS 26.5) — screen shows `increment(41) = 42`
  - `just ios-generate` (`build-guest-wasm` then `tuist generate`) added, mirroring `exp-mbt-wasmkit-ios`'s recipe of the same name. `iosApp/*.xcodeproj`, `*.xcworkspace`, `Derived/` already covered by the `.gitignore` patterns stubbed during Milestone 0
  - **Deliberately no iOS CI job**: an Xcode+Tuist+simulator build is significantly more setup than `shared-lint`/`shared-test`'s plain Gradle+Java steps, and this wasn't asked for. Left as an open question below, not a silent omission
- [x] **Recorded call-overhead measurements**, same methodology as `exp-mbt-wasmkit-ios` (one-time setup cost separated from per-call cost, 100,000 iterations, `increment` vs `move_point` for the scalar-vs-boxed-tuple comparison)
  - `GuestBenchmark.kt` (`commonMain`): `benchmarkIncrement`/`benchmarkMovePoint`, timed with `kotlin.time.TimeSource.Monotonic` (portable across targets, unlike Swift's `CFAbsoluteTimeGetCurrent` which `exp-mbt-wasmkit-ios` used). `move_point`'s benchmark uses the low-level embedding API directly, same as `GuestMemoryTest`, for the same reason (`GuestServiceImpl` can't be reused across a pointer read)
  - A small portability snag: `kotlin.text.String.format` (used for the JVM-only print test) doesn't exist on Kotlin/Native — replaced with a manual `round3()` helper (`kotlin.math.round`) shared by both platform-specific print tests, rather than maintaining two different formatting implementations
  - `GuestBenchmarkTest.kt` (`commonTest`, 1,000 iterations): sanity-checks both benchmarks run and report non-negative timings on every target — not a performance assertion (flaky by nature), just confirms the code path works
  - `GuestBenchmarkJvmTest.kt` / `GuestBenchmarkIosSimulatorArm64Test.kt` (platform-specific, 100,000 iterations): print formatted results via `println`, captured with `--tests "..." --rerun -i`
  - `iosApp/Sources/ContentView.swift` now also displays both benchmarks (mirroring `exp-mbt-wasmkit-ios`'s exact on-screen format) — rebuilt, reinstalled, relaunched, screenshotted on the same `iPhone 17` simulator to get an actual app-embedded number, not just a bare test-binary one
  - **Results** (100,000 calls each; `exp-mbt-wasmkit-ios`'s WasmKit numbers from the same simulator generation, for comparison):

    | Target | `increment` (µs/call) | `move_point` (µs/call) | ratio |
    | :--- | ---: | ---: | ---: |
    | JVM (`jvmTest`) | 0.563 | 1.449 | ~2.6x |
    | `iosSimulatorArm64` (bare Kotlin/Native test) | 3.817 | 20.015 | ~5.2x |
    | `iosSimulatorArm64` (via `iosApp/`, app-embedded) | 3.602 | 19.459 | ~5.4x |
    | `exp-mbt-wasmkit-ios` (WasmKit, via its iOS app) | 6.378 | 32.215 | ~5.0x |

  - **Reading**: the scalar-vs-boxed-tuple ratio (~5x) is nearly identical between Chasm and WasmKit — confirms the earlier conclusion that this multiplier comes from MoonBit's own guest-side allocator, not from either host. Chasm on Kotlin/Native is consistently faster in absolute terms than WasmKit here (~1.7x for `increment`, ~1.6x for `move_point`), and the JVM target is faster again on top of that (unsurprising: JIT-compiled bytecode vs. an interpreter). All four numbers clear the same "modest 2D game" headroom bar `exp-mbt-wasmkit-ios` set (a few dozen draw calls + input polling per frame is one to two orders of magnitude below even the slowest number here) — on this evidence, call overhead is not a blocker for either host

## Milestone 2: Android emulator verification

Goal: this is the project's actual reason for existing — Chasm was chosen over WasmKit specifically because it could in principle be **one host implementation for both iOS and Android**. Milestone 1 only verified iOS and used the `jvm` target as a desktop-JVM stand-in for Android; this milestone verifies the real thing on an actual Android emulator (ART, not just JVM bytecode on a desktop JVM).

- [x] Pinned the JDK via `kotlin { jvmToolchain(21) }` (matches CI's `actions/setup-java` version) rather than relying on the invoking shell's `JAVA_HOME` — prompted by a user suggestion to use a `.env.local` file; recommended the Gradle-native toolchain mechanism instead, since it needs nothing sourced and fixes compilation-JDK selection for every contributor and CI, not just this sandbox's session-specific `./gradlew` launcher quirk (which jvmToolchain doesn't and can't fix, since that's about finding a JVM to *start* Gradle at all, a separate concern from what JDK *compilation* uses). Needed `org.gradle.toolchains.foojay-resolver-convention` in `settings.gradle.kts` for Gradle to auto-provision JDK 21 when only JDK 26 was installed locally
- [x] **Added the Android target to `shared`**, reusing 100% of the existing `commonMain` code (`GuestService`, `createGuestService()`, `GuestBenchmark`) with zero duplication — this was the whole point of trying `androidTarget()` inside the existing KMP module rather than a separate Android-only module (Chasm's own `consumer-android` example uses the latter, but that's not what this project needs to answer)
  - **Immediate, real complication**: AGP 9.0+ made `com.android.library` incompatible with `org.jetbrains.kotlin.multiplatform` outright (compile-time error, not a warning) — the KMP+Android integration model changed to a dedicated `com.android.kotlin.multiplatform.library` plugin, with `androidTarget()` replaced by an `android { }` block *inside* `kotlin { }`, and source sets renamed (`androidMain` unchanged, but `androidTest`/`androidUnitTest` become `androidDeviceTest`/`androidHostTest`, both opt-in via `withDeviceTestBuilder`/`withHostTestBuilder`)
  - Verified this was real (not a guess) by checking Google's Maven repo directly for the plugin id and version history before using it, same discipline as the earlier `actions/setup-java` SHA mistake and the WebFetch/kotlin-multiplatform-dev-docs lesson
  - The earlier-flagged risk (Chasm's KMP `chasm` artifact publishes no explicit Android variant on Maven Central) turned out not to matter: Gradle resolved it fine for the Android target, presumably via its JVM variant
  - `shared/src/androidMain/kotlin/.../Platform.android.kt` added the missing `actual fun platformName()` the new target needed (same `expect`/`actual` gap Kotlin already caught for `jvm`/`iosSimulatorArm64` at their own introduction)
- [x] Scaffolded `androidApp/`, a plain `com.android.application` module (`android:minSdk=24`, `compileSdk=36`, matching the installed SDK) depending on `project(":shared")` — no Compose, no XML layout, a single `Activity` with a programmatic `TextView`, mirroring `iosApp/`'s minimalism. Unlike Swift/Kotlin-Native interop, calling `shared`'s Kotlin from Android needs no special bridging (`createGuestService()`, not `GuestServiceFactoryKt.createGuestService()` — the `Kt`-suffixed-class convention is Kotlin/Native's Objective-C export mechanism specifically, irrelevant for plain Kotlin-to-Kotlin calls)
- [x] **`commonTest`'s existing suite (`GuestServiceTest`, `GuestServiceFactoryTest`, `GuestMemoryTest`, `GuestBenchmarkTest`, `PlatformTest`) ran against Android with no new test code needed** — first against `androidHostTest` (fast, local JVM against Android SDK stub jars) — all passed immediately
  - Then against `androidDeviceTest` (the actual instrumented run on a real Android emulator) — this **did** need fixing: the new AGP plugin declares an `androidx.test.runner.AndroidJUnitRunner` instrumentation in the test manifest automatically, but doesn't add the artifact that class lives in. Silently reported `tests="0"` with `BUILD SUCCESSFUL` at first — no error surfaced until checking `adb logcat`, which showed `ClassNotFoundException` for the runner class. Added `androidx.test:runner` + `androidx.test.ext:junit` to the `androidDeviceTest` source set's dependencies; all 6 tests then ran and passed for real, confirmed via the JUnit XML file's actual `<testcase>` entries (not just "BUILD SUCCESSFUL", which had already been a false signal once this session)
- [x] Booted `Medium_Phone` (Android 16 platform, `arm64-v8a`, already present locally) headlessly, ran `./gradlew :shared:connectedAndroidDeviceTest` against it, then separately installed and launched `androidApp` and screenshotted the running app — both the instrumented tests and the real app confirm `increment`/`move_point` work correctly on-device
- [x] Added `GuestBenchmarkAndroidDeviceTest` (100,000 iterations, printed via `println`, captured from `adb logcat`) alongside the existing per-target benchmark tests
- [x] **Call-overhead results, now across every target** (100,000 calls each):

  | Target | `increment` (µs/call) | `move_point` (µs/call) | ratio |
  | :--- | ---: | ---: | ---: |
  | JVM (desktop, `jvmTest`) | 0.563 | 1.449 | ~2.6x |
  | Android (`Medium_Phone` emulator, instrumented test) | 6.645 | 14.417 | ~2.2x |
  | Android (`Medium_Phone` emulator, via `androidApp`) | 8.286 | 13.656 | ~1.6x |
  | `iosSimulatorArm64` (bare Kotlin/Native test) | 3.817 | 20.015 | ~5.2x |
  | `iosSimulatorArm64` (via `iosApp/`, app-embedded) | 3.602 | 19.459 | ~5.4x |
  | `exp-mbt-wasmkit-ios` (WasmKit, via its iOS app) | 6.378 | 32.215 | ~5.0x |

  - **Reading**: Android's `increment` cost (6.6–8.3 µs/call) lands in almost the same range as WasmKit's iOS number (6.378 µs/call) — both far behind desktop JVM (expected: emulator + ART interpretation overhead vs. a JIT-warmed desktop JVM) and behind Kotlin/Native too. More interesting: Android's scalar-vs-boxed-tuple ratio (~1.6–2.2x) is much lower than iOS's (~5x) — ART evidently doesn't pay the same relative penalty for the guest-heap-allocation + memory-read path that Kotlin/Native and WasmKit both do, though the absolute `move_point` cost is still in the same ballpark across every target (13–32 µs/call). Every number here, on every target, is still one to two orders of magnitude below what a modest 2D game needs per frame — the project's central question (can one Chasm/KMP host answer for iOS *and* Android acceptably) now has real, on-device evidence behind it, not just an iOS answer plus a desktop-JVM proxy for Android
- [ ] Physical device measurements (both iOS and Android are simulator/emulator-only so far, same caveat `exp-mbt-wasmkit-ios` already carries for its own iOS numbers)

## Open questions

- [x] ~~Where does Chasm's Gradle plugin expect to read the `.wasm` input from~~ — `src/commonMain/resources/<name>.wasm` for a `kotlin.multiplatform` module, confirmed via `docs/plugin.md` and the plugin's own multiplatform example, both read directly from the `CharlieTap/chasm` repo at the `1.7.0` tag
- [x] ~~Does Chasm support only core `wasm`, or also `wasm-gc`?~~ — both; Chasm's README lists Wasm GC as a supported proposal (✅). Not exercised yet since `guest/`'s current exports don't need it, but this means (unlike WasmKit/Chicory) there's no forced reason to stay off `wasm-gc` if a later spike wants it
- [x] ~~How does Chasm's generated binding surface handle a MoonBit function returning a heap-boxed value~~ — it doesn't; see the Milestone 1 entry above
- [ ] Is an iOS CI job (Xcode + Tuist + simulator build/run) worth the setup cost, given `exp-mbt-wasmkit-ios` doesn't have one either? Not decided; not blocking anything right now
- [ ] Same question for an Android CI job (would need an emulator runner, e.g. `reactivecircus/android-emulator-runner`) — not decided, not blocking
- [ ] Why is Android's scalar-vs-boxed-tuple ratio (~2x) so much lower than iOS's (~5x)? Not investigated — a guess (ART's interpreter/JIT handling the allocation+memory-read path more cheaply relative to its own scalar-call baseline than Kotlin/Native's runtime does) isn't verified
